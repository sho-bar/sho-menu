{
    "name": "sho-bar/sho-menu",
    "autoload": {
        "psr-4": {
            "ShoMenu\\": "app/"
        }
    },
    "authors": [
        {
            "name": "SerhiiCho",
            "email": "serhiicho@protonmail.com"
        }
    ],
    "require-dev": {
        "symfony/var-dumper": "^7.0",
        "laravel/pint": "^1.15"
    },
    "require": {
        "php": "^8.2",
        "serhii/tiny-logger": "^2.1",
        "yahnis-elsts/plugin-update-checker": "^5.4"
    },
    "scripts": {
        "build": [
            "composer install --no-dev --optimize-autoloader",
            "yarn prod",
            "git add .",
            "git commit -m 'Build release for production'"
        ],
        "pint": "./vendor/bin/pint"
    }
}
